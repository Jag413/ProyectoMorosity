Public Class AgregarPeticion
    Public Sub New()
        InitializeComponent()
    End Sub

    Private Sub OnClic_Enviar(sender As Object, e As RoutedEventArgs)

        'enviar datos a la cola
        'llamar al controlador equifax para que inserte en cola
        'implementar la llamada
        
        Close()
    End Sub
End Class